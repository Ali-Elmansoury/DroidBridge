[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "droidbridge"
version = "0.1.0"
description = "ADB-powered Android device management tool"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }
dependencies = [
    "click>=8.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-qt>=4.2",
]
gui = [
    "PyQt6>=6.4",
]

[project.scripts]
droidbridge = "droidbridge.cli.main:cli"
droidbridge-gui = "droidbridge.gui.app:main"

[tool.setuptools.packages.find]
include = ["droidbridge*"]

[tool.setuptools.package-data]
"droidbridge" = ["resources/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
qt_api = "pyqt6"
